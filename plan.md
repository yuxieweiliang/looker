# 用户登录状态下的丝滑体验方案

## Context

当前 Looker 项目需要支持：
- 用户未登录时可以浏览公开内容（首页、广场、话题等）
- 用户登录后可以享受个性化内容和服务（点赞、收藏、关注等）
- 未登录用户点击需要登录的功能时，优雅地引导用户登录
- 登录/未登录状态切换流畅，不影响用户体验

---

## 现状分析

### 接口公开性（已验证）

#### 公开接口（无需登录）

| 接口 | 路径 | 说明 |
|------|------|------|
| 获取动态列表 | `GET /feeds` | 游客可访问 |
| 获取动态详情 | `GET /feeds/:id` | 游客可访问，评论也公开 |
| 获取瀑布流图片 | `GET /photos` | 游客可访问 |
| 获取话题列表 | `GET /topics` | 游客可访问 |
| 搜索 | `GET /search` | 游客可访问 |
| 热门搜索 | `GET /search/hot` | 游客可访问（改为公开） |
| 搜索建议 | `GET /search/suggestions` | 游客可访问（改为公开） |

#### 需要登录的接口

| 接口 | 路径 | 说明 |
|------|------|------|
| 获取话题详情 | `GET /topics/:name` | 需要登录 |
| 关注话题 | `POST /topics/:name/follow` | 需要登录 |
| 发布动态 | `POST /feeds` | 需要登录 |
| 点赞/取消点赞 | `POST /likes` | 需要登录 |
| 收藏/取消收藏 | `POST /bookmarks` | 需要登录 |
| 关注/取消关注 | `POST /follows` | 需要登录 |
| 评论 | `POST /comments` | 需要登录（查看评论不需要） |

### 页面分类

| 页面 | 未登录行为 | 登录后额外功能 |
|------|-----------|--------------|
| 首页 | 浏览公开内容 | 显示点赞/收藏状态，个性化推荐 |
| 广场 | 浏览公开内容 | 显示点赞/收藏状态 |
| 话题列表 | 浏览话题 | 关注话题 |
| 话题详情 | 浏览动态 | 发布动态、参与讨论 |
| 动态详情 | 浏览内容 + 评论(公开) | 点赞、收藏、评论(带身份) |
| 个人中心 | 显示登录入口 | 完整功能 |
| 搜索 | 搜索结果公开 | 历史记录、个性化推荐 |

### 需要登录的功能点

| 功能 | 触发场景 | 当前处理 |
|------|---------|---------|
| 点赞 | 点击点赞按钮 | **无前置检查**，API 失败后才提示 |
| 收藏 | 点击收藏按钮 | **无前置检查**，API 失败后才提示 |
| 评论 | 提交评论 | **无前置检查**，API 失败后才提示 |
| 关注 | 点击关注按钮 | **无前置检查**，API 失败后才提示 |
| 发布 | 点击发布按钮 | 跳转话题页，未拦截 |
| 查看个人中心 | 进入个人中心 | 静默获取数据，失败后引导登录 |
| 消息/草稿箱/钱包 | 点击功能入口 | **无前置检查**，直接跳转或失败 |

### 当前问题

1. **被动响应**：只有 API 请求被拒绝（401/403）后才提示登录
2. **缺少统一检查**：各页面/组件直接调用 API，无登录状态前置检查
3. **体验不佳**：用户点击后才知道需要登录

---

## 解决方案

### 一、页面内容展示策略

#### 1. 首页/广场（无需登录即可浏览）

**未登录状态**：
- 展示公开的瀑布流内容
- 点赞、收藏按钮显示但为灰色禁用状态，hover/click 提示"登录后可见"
- 不显示个性化推荐区域

**已登录状态**：
- 展示公开内容 + 个性化状态（已点赞红色、已收藏标记）
- 显示个性化推荐区域
- 用户可进行点赞、收藏等操作

#### 2. 动态详情页

**未登录状态**：
- 展示完整内容
- 评论展示公开内容
- 点赞/收藏/关注按钮禁用，提示"登录后可见"
- 评论输入框显示"登录后发表评论"

**已登录状态**：
- 完整功能可用
- 显示当前用户的点赞/收藏/关注状态

#### 3. 个人中心页

**未登录状态**：
- 顶部显示"登录查看更多"引导区域
- 展示部分公开内容（如热门博主推荐）
- 功能入口点击后引导登录

**已登录状态**：
- 显示用户完整信息
- 所有功能可用

#### 4. 话题列表页

**未登录状态**：
- 展示话题列表
- 已关注状态显示为灰色"登录后可见"
- 点击话题进入话题详情页

**已登录状态**：
- 显示真实关注状态
- 可关注/取消关注

---

### 二、登录弹窗触发时机

#### 原则
- **浏览类功能**：不拦截，用户可自由浏览
- **交互类功能**：点击时检查，未登录则引导
- **敏感操作**：如支付、设置，需要强制登录

#### 触发场景分级

| 级别 | 场景 | 处理方式 |
|------|------|---------|
| P0 强制 | 支付、账号设置、实名认证 | 弹窗引导，无法跳过 |
| P1 重要 | 点赞、收藏、评论、关注 | 弹窗引导，可取消 |
| P2 普通 | 发布、转发、分享 | Toast 提示，可取消 |
| P3 提示 | 查看个人中心、消息 | 静默跳转登录页 |

#### 按钮状态

- **未登录状态**：点赞/收藏按钮显示灰色禁用状态
- **点击时**：弹出登录提示弹窗
- **跳转页面**：跳转全屏登录页 `/pages/login/login`

#### 具体触发时机

```
用户点击点赞（灰色禁用） → 弹出登录弹窗 →
  "登录后即可点赞"
  [去登录] [取消]
  点击去登录 → 跳转 /pages/login/login
```

```
用户点击收藏（灰色禁用） → 弹出登录弹窗 →
  "登录后即可收藏"
  [去登录] [取消]
```

---

### 三、技术实现

#### 1. 统一登录状态检查函数

**文件**: `client/src/utils/auth.ts`

```typescript
/**
 * 检查用户是否已登录
 */
export function isLoggedIn(): boolean {
  const token = uni.getStorageSync('token')
  return !!token
}

/**
 * 检查登录状态并引导登录
 * @param message 提示文案
 * @returns boolean true 表示已登录，false 表示未登录（已引导）
 */
export function checkLoginAndGuide(message = '请先登录后再操作'): boolean {
  if (isLoggedIn()) {
    return true
  }

  uni.showModal({
    title: '提示',
    content: message,
    confirmText: '去登录',
    cancelText: '取消',
    success: (res) => {
      if (res.confirm) {
        uni.navigateTo({ url: '/pages/login/login' })
      }
    }
  })
  return false
}
```

#### 2. 各页面/组件的登录引导接入

**详情页 - 点赞按钮**

```typescript
const onLike = () => {
  if (!checkLoginAndGuide('登录后即可点赞')) {
    return
  }
  doLike()
}
```

**详情页 - 评论输入**

```typescript
const onCommentFocus = () => {
  if (!checkLoginAndGuide('登录后发表评论')) {
    return
  }
}
```

**个人中心 - 功能入口**

```typescript
const onClickMenu = (menu: string) => {
  if (!isLoggedIn()) {
    checkLoginAndGuide('登录后即可使用该功能')
    return
  }
}
```

---

### 四、登录成功后的状态同步

#### 1. 事件机制

```typescript
// 登录成功时
uni.$emit('loginSuccess')

// 各页面监听
uni.$on('loginSuccess', () => {
  fetchData()
})
```

#### 2. 需要刷新的页面

| 页面 | 刷新内容 |
|------|---------|
| 首页 | 瀑布流 + 个性化状态 |
| 广场 | 动态列表 + 状态 |
| 详情页 | 点赞/收藏/关注状态 |
| 个人中心 | 用户信息 + 功能状态 |

---

### 五、实施步骤

#### Phase 1: 服务端改造
1. 修改 `SearchController.ts` - 移除 `@Authorized()` 从 `getHotSearches` 和 `getSuggestions`

#### Phase 2: 工具函数建设
1. 创建 `client/src/utils/auth.ts` - 登录状态检查和引导函数

#### Phase 3: 核心页面改造
1. 详情页 `detail.vue` - 点赞、收藏、评论、关注的登录检查
2. 个人中心 `profile.vue` - 功能入口的登录检查
3. 首页 `home.vue` - 点赞/收藏按钮状态和登录引导

#### Phase 4: 组件改造
1. FeedCard.vue - 点赞/收藏的登录检查
2. CommentItem.vue - 点赞的登录检查

#### Phase 5: 状态同步
1. 登录成功事件 `loginSuccess` 的触发和监听
2. 各页面数据刷新逻辑

---

### 六、验证清单

- [ ] 未登录可浏览首页、广场、话题
- [ ] 未登录时点赞/收藏按钮禁用并提示
- [ ] 未登录时点击功能入口弹窗引导
- [ ] 登录后页面自动刷新显示个性化内容
- [ ] 登出后页面状态正确重置
- [ ] 各页面之间切换流畅

---

## 讨论要点（已确认）

| 问题 | 决策 |
|------|------|
| 热搜/搜索建议接口 | ✅ 改为公开，无需登录 |
| 评论 | ✅ 需要登录才能评论，查看不需要 |
| 点赞/收藏按钮 | ✅ 灰色禁用，点击弹出登录提示 |
| 登录跳转页面 | ✅ 跳转全屏登录页 `/pages/login/login` |